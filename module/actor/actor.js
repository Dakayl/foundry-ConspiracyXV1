/* global Actor */

/**
 * Extend the basic Item with some very simple modifications.
 * @extends {Actor}
 */
export class ConspiracyActor extends Actor {
    prepareData () {
        super.prepareData()
    }    
}